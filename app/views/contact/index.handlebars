{{> banner-other}}
<div class="page-contact">
  <div class="container">
    <div class="cards">
      <div class="row card">
        <div class="col-md-1"></div>
        <div class="col-md-11">
          <div class="name">{{__ 'contact-card1-name'}}</div>
          <p class="text">{{__ 'contact-card1-text1'}}</p>
          <p class="text">{{__ 'contact-card1-text2'}}</p>
          <p class="text">{{__ 'contact-card1-text3'}}</p>
        </div>
      </div>
    </div>
  </div>
</div>

{{> scripts}}